#include "PluginManager.h"
#include <iostream>

void PluginManager::loadPlugins(const std::string& folder) {
    std::cout << "Loading plugins from " << folder << "..." << std::endl;
    // пока просто выводим сообщение, без загрузки
}

